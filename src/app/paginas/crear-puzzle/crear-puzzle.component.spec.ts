/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { CrearPuzzleComponent } from './crear-puzzle.component';

describe('CrearPuzzleComponent', () => {
  let component: CrearPuzzleComponent;
  let fixture: ComponentFixture<CrearPuzzleComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ CrearPuzzleComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(CrearPuzzleComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*//////////////